version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install -g
  build:
    commands:
      - cd novavolt-energyusage
      - sls deploy -s qa
artifacts:
  files:
    - '**/*'
  base-directory: .serverless

