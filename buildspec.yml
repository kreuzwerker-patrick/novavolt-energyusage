version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install serverless -g
      - npm install 
  build:
    commands:
      - cd novavolt-energyusage
      - sls update --stage qa -v
artifacts:
  files:
    - '**/*'
  base-directory: .serverless

